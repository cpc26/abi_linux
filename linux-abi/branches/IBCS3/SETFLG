# This file should not be modified.

EXTRA_CFLAGS += -DIBCS_VERSION="\"3.8\""

include $(ABI_DIR)/CONFIG

ifeq ($(BINFMT_XOUT_X286),Y)
EXTRA_CFLAGS += -DCONFIG_BINFMT_XOUT_X286
endif

ifeq ($(ABI_TRACE),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_TRACE
endif

ifeq ($(ABI_SPX),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_SPX
endif

ifeq ($(ABI_SIGRETURN),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_SIGRETURN
endif

ifeq ($(ABI_XTI),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_XTI
endif

ifeq ($(ABI_XTI_OPTMGMT),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_XTI_OPTMGMT
endif

ifeq ($(ABI_TLI_OPTMGMT),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_TLI_OPTMGMT
endif

ifeq ($(ABI_ELFMARK),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_ELFMARK
endif

ifeq ($(ABI_SHINOMAP),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_SHINOMAP
endif

ifeq ($(ABI_PROC),Y)
EXTRA_CFLAGS += -DCONFIG_ABI_PROC
endif

EXTRA_CFLAGS += -D_KSL=$(SUBLEVEL)
EXTRA_AFLAGS += -D_KSL=$(SUBLEVEL)
